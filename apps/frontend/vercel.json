{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "pnpm run build",
  "installCommand": "corepack enable && corepack prepare pnpm@10.6.1 --activate && pnpm install",
  "outputDirectory": ".next"
} 